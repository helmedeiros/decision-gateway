version: "2"
linters:
  exclusions:
    rules:
      # Test bodies: deferred resp.Body.Close() is the canonical Go
      # pattern; the err is unrecoverable inside the test.
      - path: _test\.go$
        linters: [errcheck]
        text: 'Error return value of `\(?.*\)?\.Close` is not checked'
